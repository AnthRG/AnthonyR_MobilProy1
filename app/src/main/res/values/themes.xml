<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileFirstProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/whatsapp_green</item>
        <item name="colorPrimaryVariant">@color/whatsapp_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/whatsapp_teal</item>
        <item name="colorSecondaryVariant">@color/whatsapp_teal</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/whatsapp_teal</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MobileFirstProject" parent="Base.Theme.MobileFirstProject" />
</resources>